{
  "name": "estudo-flex-languages",
  "version": "0.9.1",
  "private": true,
  "type": "module",
  "scripts": {
    "audit": "node scripts/audit.mjs",
    "test": "node --test tests/*.test.js",
    "check": "npm run audit && npm test && npm run backend:check",
    "backend:check": "node --check backend/src/server.js && node --check backend/src/gemini-client.js"
  },
  "engines": {
    "node": ">=20"
  }
}
