{
  "name": "estudo-flex-languages",
  "version": "0.6.1",
  "private": true,
  "type": "module",
  "scripts": {
    "audit": "node scripts/audit.mjs",
    "test": "node --test tests/*.test.js",
    "check": "npm run audit && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
